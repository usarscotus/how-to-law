---
import BaseLayout from '~/layouts/BaseLayout.astro';
import { getLessonOrder, modules } from '~/lib/utils';
import { getCollection } from 'astro:content';

const lessons = await getCollection('classroom');
const moduleMeta = modules.map((module) => {
  const moduleLessons = lessons
    .filter((lesson) => {
      const lessonModule = lesson.data.module ?? lesson.slug.split('/')[0];
      return lessonModule === module.id;
    })
    .sort((a, b) => getLessonOrder(a) - getLessonOrder(b));
  return {
    ...module,
    lessonCount: moduleLessons.length,
    nextLesson: moduleLessons[0]
      ? `/how-to-law/classroom/${module.id}/${moduleLessons[0].slug.split('/')[1]}/`
      : '/how-to-law/classroom/'
  };
});
const activeModules = moduleMeta.filter((module) => module.lessonCount > 0);
---
<BaseLayout
  title="How to Law"
  description="Friendly, community-written lessons covering the building blocks of United States law."
>
  <section class="bg-background">
    <div class="mx-auto flex max-w-4xl flex-col gap-10 px-4 py-16 sm:px-6 lg:px-8">
      <div class="space-y-6 text-center sm:text-left">
        <h1 class="text-4xl font-bold tracking-tight text-foreground sm:text-5xl">
          Learn U.S. law basics — free &amp; community-made
        </h1>
        <p class="text-lg text-foreground/80">
          Learn alongside neighbors, classmates, and fellow volunteers. Our plain-language lessons walk through essential U.S. legal concepts and procedures so anyone can build confidence navigating everyday civic life.
        </p>
        <div class="flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:items-center">
          <a
            href="/how-to-law/classroom/"
            class="inline-flex items-center justify-center rounded-full bg-accent px-6 py-3 text-base font-semibold text-accent-foreground shadow-subtle transition focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-accent"
          >
            Start learning
          </a>
          <a
            href="#modules"
            class="inline-flex items-center justify-center rounded-full border border-border px-6 py-3 text-base font-semibold text-foreground transition hover:border-accent/60 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-accent"
          >
            Browse topics
          </a>
          <a
            href="/how-to-law/search/"
            class="inline-flex items-center justify-center rounded-full border border-border px-6 py-3 text-base font-semibold text-foreground transition hover:border-accent/60 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-accent"
          >
            Search lessons
          </a>
        </div>
      </div>
      <div class="grid gap-6 sm:grid-cols-[minmax(0,1fr)] sm:gap-6 lg:grid-cols-[minmax(0,2fr)_minmax(0,1fr)] lg:items-start">
        <div class="rounded-3xl border border-border bg-card/80 p-6 shadow-subtle">
          <h2 class="text-lg font-semibold text-foreground">How it works</h2>
          <dl class="mt-6 grid gap-5 sm:grid-cols-3">
            <div class="flex flex-col gap-2 rounded-2xl bg-background/70 p-4 text-left">
              <dt class="text-sm font-semibold text-foreground">Learn together</dt>
              <dd class="text-sm text-foreground/80">
                Short lessons share community insights with links to primary sources and further reading.
              </dd>
            </div>
            <div class="flex flex-col gap-2 rounded-2xl bg-background/70 p-4 text-left">
              <dt class="text-sm font-semibold text-foreground">Practice skills</dt>
              <dd class="text-sm text-foreground/80">
                Quizzes, examples, and checklists help you practice applying legal principles to real-life scenarios.
              </dd>
            </div>
            <div class="flex flex-col gap-2 rounded-2xl bg-background/70 p-4 text-left">
              <dt class="text-sm font-semibold text-foreground">Share knowledge</dt>
              <dd class="text-sm text-foreground/80">
                Contribute ideas, flag updates, and celebrate wins as we keep the guide current and approachable.
              </dd>
            </div>
          </dl>
        </div>
        <div class="rounded-3xl border border-destructive/40 bg-destructive/5 p-6 shadow-subtle">
          <h2 class="text-base font-semibold text-destructive">Important reminder</h2>
          <p class="mt-3 text-sm text-destructive/90">
            Not legal advice. Not affiliated with any court.
          </p>
          <p class="mt-2 text-sm text-foreground/80">
            Always consult a licensed attorney for guidance on your specific situation. This site is for learning and community support only.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section id="modules" class="border-t border-border/50 bg-card/40">
    <div class="mx-auto max-w-6xl px-4 py-16 sm:px-6 lg:px-8">
      <h2 class="text-2xl font-semibold text-foreground">Browse the classroom</h2>
      <p class="mt-2 text-foreground/70">
        Pick a topic to start exploring U.S. legal foundations, rights, and procedures.
      </p>
      <div class="mt-10 grid gap-8 md:grid-cols-2">
        {activeModules.map((module) => (
          <article class="flex h-full flex-col justify-between rounded-3xl border border-border bg-background/80 p-6 shadow-subtle">
            <div class="space-y-3">
              <h3 class="text-xl font-semibold text-foreground">{module.title}</h3>
              <p class="text-sm text-foreground/80">{module.description}</p>
              <p class="text-xs uppercase tracking-wide text-muted">{module.lessonCount} lesson{module.lessonCount === 1 ? '' : 's'}</p>
            </div>
            <a
              href={module.nextLesson}
              class="mt-6 inline-flex items-center justify-center rounded-full bg-accent/10 px-4 py-2 text-sm font-semibold text-accent transition hover:bg-accent/15 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-accent"
            >
              Start this module
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="border-t border-border/40 bg-background">
    <div class="mx-auto max-w-6xl px-4 py-16 sm:px-6 lg:px-8">
      <div class="grid gap-8 lg:grid-cols-2 lg:items-center">
        <div class="space-y-4">
          <h2 class="text-2xl font-semibold text-foreground">Learning support</h2>
          <ul class="space-y-3 text-sm text-foreground/80">
            <li class="flex items-start gap-3">
              <span class="mt-1 inline-flex h-2 w-2 shrink-0 rounded-full bg-accent" aria-hidden="true" />
              Friendly summaries highlight key statutes, constitutional principles, and court structures.
            </li>
            <li class="flex items-start gap-3">
              <span class="mt-1 inline-flex h-2 w-2 shrink-0 rounded-full bg-accent" aria-hidden="true" />
              Scenario walk-throughs show how legal steps unfold from first questions through resolution.
            </li>
            <li class="flex items-start gap-3">
              <span class="mt-1 inline-flex h-2 w-2 shrink-0 rounded-full bg-accent" aria-hidden="true" />
              Search and glossary tools make it easy to revisit definitions and explore related lessons.
            </li>
          </ul>
        </div>
        <div class="rounded-3xl border border-border bg-card/80 p-6 shadow-subtle">
          <h3 class="text-lg font-semibold text-foreground">Community-driven resources</h3>
          <ul class="mt-4 space-y-3 text-sm text-foreground/80">
            <li>
              <strong class="text-foreground">Lesson feedback:</strong> share suggestions to help keep content accessible and accurate.
            </li>
            <li>
              <strong class="text-foreground">Glossary popovers:</strong> focus or hover on key terms for friendly definitions and citations.
            </li>
            <li>
              <strong class="text-foreground">Downloadable guides:</strong> print checklists and flowcharts to support group study or workshops.
            </li>
          </ul>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
